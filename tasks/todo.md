# 待办任务

## 高优先级：Phase 1 本地可信 MVP

- [ ] 确认首发最低 macOS 与 Windows 版本。
- [ ] 用 ScreenCaptureKit 实现 macOS 权限、显示器枚举和单次截图。
- [ ] 用 Windows.Graphics.Capture 实现 Windows 捕获适配器。
- [ ] 在 macOS 菜单栏和 Windows 各方向任务栏上真机验收稳定内容去重，覆盖 Retina/DPI、自动隐藏、全屏和多显示器。
- [ ] 在 macOS 与 Windows 真机验收锁屏、快速用户切换、休眠/现代待机、唤醒、空闲阈值变更及多重阻塞事件顺序。
- [ ] 将“今日数量”和存储占用统计切换为 SQLite 与可验证文件状态。
- [ ] 实现缩略图失败后的后台重建和数据库状态更新。
- [ ] 在 macOS 与 Windows 真机验收托盘图标、动态状态、开始/暂停/停止、缺少权限提示、主窗口打开和退出行为。

## 中优先级：Phase 2 个人服务器手动上传

- [ ] 跟进 ESLint 的 `minimatch` / `brace-expansion` 开发依赖告警，等待上游提供兼容修复后升级；生产依赖审计当前为 0。
- [ ] 在隔离 SFTP 服务器验证错误指纹、错误密钥、不可写目录和中断写入。
- [ ] 增加失败项的显式重试与取消界面。
- [ ] 验证 Windows Credential Manager 和私钥文件权限行为。
- [ ] 增加上传队列崩溃恢复，将遗留 `uploading` 转为等待用户处理。
- [ ] 完成私钥、口令、图片和远端路径日志泄露测试。

## 低优先级：Phase 3 旅程增强

- [ ] 本地 OCR 和本地全文搜索。
- [ ] 应用级隐私遮挡、收藏和标签。
- [ ] 旅程包导出。

## 待确认的产品决策

- [ ] MVP 是否同时捕获多个显示器。
- [ ] 是否在第二阶段支持多个个人服务器配置。
- [ ] 是否增加失败项重试、取消和带宽限制。
- [ ] 是否增加经过审核的远端内容摘要验证。
- [ ] 首发采用官网下载还是应用商店。
- [ ] 选择项目开源或商业许可证。
