# 已完成任务

## 2026-08-17 - 上传队列崩溃恢复

- 应用启动时将遗留的 `pending`/`uploading` 上传项目转为 `failed(interrupted)`，批次转为 `partial_failed`，不自动重放网络请求。
- 时间线启动后会发现最近尚未处理的中断批次，明确提示“等待处理”，并复用失败项重试入口。
- 创建重试批次后，旧的中断批次不再重复提醒；增加 SQLite 恢复查询和回归测试。

## 2026-08-13 - 上传失败项重试与取消界面

- 时间线上传提示新增“取消上传”：尚未开始的项目标记为 `cancelled`，当前传输项目允许完成后记录真实的成功或失败结果。
- 时间线上传失败后新增“重试失败项”，只为原批次的失败截图创建新手动批次，保留原批次历史和成功项目状态。
- Rust 新增重试、取消 Tauri 命令与数据库状态转换；增加取消状态和失败项新批次回归测试。
- 更新 API 与产品设计文档，明确取消和重试的状态语义。

## 2026-08-13 - 依赖告警修复与隔离 SFTP 异常场景验收

- 上游兼容修复发布后更新 `package-lock.json`：`brace-expansion` 升至 1.1.18 / 5.0.9，另更新 `js-yaml` 至 4.3.1、`nanoid` 至 3.3.18；未升级 ESLint 大版本。
- `npm audit --omit=dev` 与完整 `npm audit` 均为 0 个漏洞。
- 在一次性本地隔离 SFTP 服务器验证：正确指纹可建立会话；错误指纹被拒绝（SSH 状态 255）；错误私钥在禁用 SSH agent 后被拒绝（SSH 状态 255）；不可写目录返回 `Permission denied`；中断传输留下 261120 字节 `.part` 临时文件。
- 测试使用随机本地端口、临时 SSH 密钥和临时目录，测试结束后关闭服务器；未连接个人或生产服务器。

## 2026-07-31 - SQLite 与可验证文件状态库存统计

- “今日数量”改为按当前系统时区计算 UTC 日边界，并以 SQLite `captured_at_utc` 筛选，不再依赖文件修改时间。
- 存储占用只累计 SQLite 引用且通过 UUID 受管路径、普通文件、非符号链接、大小上限验证的原图和缩略图；原图实际大小还必须与数据库记录一致。
- 启动恢复、截图成功和删除后强制刷新统一统计，普通状态轮询使用一分钟验证缓存并在跨过本地午夜后立即失效。
- 增加 SQLite 库存读取、缺失文件、大小不一致、未索引文件和路径逃逸回归测试。

## 2026-07-31 - macOS ScreenCaptureKit 单帧捕获

- 保留显式 CoreGraphics 权限预检与申请，把可捕获显示器枚举迁移到 `SCShareableContent`，把单次截图迁移到 `SCScreenshotManager`。
- ScreenCaptureKit 回调结果在适配层内完成所有权转换，并设置十秒超时；截图调用运行在阻塞线程，不阻塞 Tauri 异步执行器。
- 新增带行填充的 BGRA 到 RGBA 转换测试及异常缓冲区测试；macOS workspace 编译通过，真实截图仍需在用户授权后交互式验收。

## 2026-07-30 - 动态托盘状态与记录控制

- 创建常驻 Tauri 托盘，菜单展示真实记录状态、锁屏/休眠/空闲暂挂原因和当前屏幕录制权限。
- 托盘开始、暂停和停止复用主窗口同一 Rust 状态机及截图调度代次，不维护第二份业务状态；菜单操作可用性随状态即时更新。
- 缺少权限时，托盘不会绕过知情说明调用系统授权，而是打开并聚焦主窗口，提示用户在那里阅读说明并申请权限。
- 增加打开主窗口和退出入口，关闭主窗口时隐藏而不销毁；状态变化通过 Tauri 事件主动刷新 React，避免从托盘操作后等待后台轮询。
- 增加运行、系统暂挂和权限缺失状态映射测试；Rust workspace 与前端检查通过，macOS 和 Windows 托盘交互仍需真机验收。

## 2026-07-30 - 系统锁屏、休眠、唤醒和空闲状态监听

- macOS 接入工作区休眠/唤醒与会话通知、屏幕锁定分布式通知和 CoreGraphics 最近输入时长；Windows 接入 WTS 会话锁定/解锁、窗口电源广播和 `GetLastInputInfo`。
- 运行状态分离用户记录意图与锁屏、休眠、空闲阻塞条件；任一条件出现即取消当前截图调度，全部清除后等待 10 秒再恢复，不补拍错过周期。
- 用户在系统暂挂期间主动暂停或停止后不会自动恢复；空闲阈值设为 0 时关闭空闲暂停。
- 平台监听安装或运行时查询失败时安全停止记录并阻止重新开始，前端显示具体暂挂原因并在活动记录期间每秒同步状态。
- 增加锁定恢复、多重条件顺序、暂挂时用户暂停、空闲启动、监听失败和零阈值测试；macOS workspace 编译、Rust 全量测试及前端检查通过，Windows 仍需真机编译与事件验收。

## 2026-07-30 - 平台感知的稳定内容重复检测

- 保留完整像素 SHA-256，同时增加版本化稳定内容摘要；只有经验证的系统边缘栏不参与重复判断，保存的无损 WebP 仍是完整屏幕。
- macOS 在 Tauri 主线程读取当前 `NSScreen` 的菜单栏顶部差值并换算为捕获像素；Windows 接入 `GetMonitorInfoW` 工作区差值，支持上下左右边缘和 DPI 比例换算。
- 排除矩形必须贴边、互不重叠且厚度不超过屏幕对应尺寸 10%；平台查询失败、矩形异常或旧记录缺少稳定摘要时退回完整像素比较。
- SQLite 迁移新增可空稳定摘要和比较策略，已有截图保持原样且升级后的首张截图作为新基线；历史恢复扫描不猜测过去的系统栏几何。
- 增加系统栏内变化、内容区单像素变化、异常矩形、显示器隔离、精确回退及上一 schema 迁移保留测试。
- 前端检查、生产构建、Rust workspace 编译和全量测试通过；macOS 交互式截图及 Windows 捕获适配完成后的真机去重仍待验收。

## 2026-07-29 - 显式选择加入的当天自动同步

- 远程服务器配置新增独立自动同步开关，迁移、新配置和界面默认均为关闭。
- 用户显式开启后默认每 30 分钟检查一次，并支持 15、30、60、120、240 分钟间隔。
- 调度器只选择本地当天尚未同步的原图，复用固定主机指纹 SFTP、确定性远端路径和单活动上传队列，并按每批 500 张分段处理。
- 自动与手动批次互斥；普通失败留待下一周期重试，私钥、凭据、认证或主机身份错误会暂停计划，需重新保存或成功测试连接后恢复。
- 设置页展示离机告知、下次执行、最近结果、失败数量和暂停原因，并提供只在已启用状态下可用的“立即同步当天图片”。
- 前端检查、生产构建、Rust workspace 编译和全量测试通过；真实 SFTP 端到端仍需隔离测试服务器验收。

## 2026-07-29 - 本地精确重复检测

- 截图在编码和写盘前统一 alpha，并对尺寸与规范化 RGBA 像素计算版本化 SHA-256。
- 后端始终只与同一设备、同一显示器最近一次成功记录比较；完全一致则不创建文件、缩略图或 SQLite 记录，前端不提供关闭开关。
- 像素变化或显示器不同都会正常保存；重复跳过后继续下一次调度，且不增加今日数量和存储占用。
- SQLite 迁移增加可空像素摘要和最近显示器记录索引；已有记录不隐式批量读取，升级后的首张截图作为新基线。
- 增加像素规范化、单像素变化、显示器隔离、迁移字段和调度统计测试。

## 2026-07-28 - 初始化项目

- 根据产品设计建立 Tauri 2、React、TypeScript、Vite 和 Rust workspace。
- 建立客户端服务边界、SQLite/PostgreSQL 迁移和 Axum 健康检查。
- 建立 Codex 项目配置、规则、文档、任务、记忆和统一脚本。
- 前端检查结果和 Rust 工具链状态见本次任务交付说明。

## 2026-07-29 - 本地 WebP 与可视时间线第一批落地

- 新截图改为保留捕获像素尺寸的普通无损 WebP 原图和 1440 像素宽缩略图，并分别原子写入本地目录。
- 时间线原图读取改用大小与 SHA-256 轻量完整性校验并移入阻塞线程，预览支持即时加载状态、受限缓存、相邻预取、物理像素级显示、适应窗口和源图 100% 缩放。
- 今日页从 SQLite 查询最新截图并直接显示对应的本地原图。
- 本地保存成功前重新读取并解码原图；缩略图失败时保留有效原图并允许按需生成。
- 时间线只索引受控目录中的 WebP；原型阶段的多格式读取分支已经清理。
- 增加只接受截图 UUID 的原图与缩略图读取命令，拒绝软链接、非普通文件、超大文件和无效 WebP。
- React 时间线在接近视口时加载缩略图，点击后展示本地原图。
- SQLite 首个迁移曾建立尚未启用的 `ai_jobs` 和 `ai_job_captures`；个人服务器上传迁移会将其移除。
- 当时的产品文案与文档切换为客户端直连 LLM；该决策已由个人服务器 SFTP 手动上传方案取代。
- 前端检查、Rust workspace 检查与测试、SQLite 迁移样例验证均通过；交互式 macOS 真机截图测试仍需手动执行。

## 2026-07-29 - 接通 SQLite 时间线

- 应用启动时创建并迁移 `electronic-journey.sqlite3`，然后将连接池注册为 Tauri 受管状态。
- 新截图写入捕获 UTC 时间、IANA 时区、显示器、本地相对路径、文件大小、SHA-256、格式和缩略图状态。
- 新截图按本地日期写入 `captures/YYYY/MM/DD` 与 `thumbnails/YYYY/MM/DD`。
- 时间线分页完全按 SQLite 的 `captured_at_utc DESC, id DESC` 查询，不再依赖文件修改时间。
- 原图和缩略图读取先从 SQLite 解析记录，再验证路径位于允许的受控目录。
- 启动时受限扫描尚未入库的新旧文件；只有历史回填无法恢复真实捕获时间时才采用文件修改时间。
- 增加 UTC 排序、稳定分页、受控相对路径和目录逃逸测试。

## 2026-07-29 - 清理原型数据与格式兼容分支

- 停止仍在运行的桌面开发应用，将现有应用数据和缓存移动到废纸篓。
- SQLite 迁移压缩为单一的本地 WebP schema，不再创建上传表或图片格式分支字段。
- 删除加密容器读取、旧目录扫描、格式枚举、旧上传服务 schema 和对应加密依赖。
- 前端时间线与文档统一为单一 WebP 原图及缩略图。
- 增加 schema 回归测试，确认不再包含原型加密、上传和格式分支字段。

## 2026-07-29 - 时间线右键与单图删除

- 时间线图片新增应用内右键菜单，可查看原图或发起单张删除。
- 删除前显示不可撤销确认，删除期间禁用重复提交。
- Rust 删除接口只接受截图 UUID，并校验 SQLite 记录和受控文件路径。
- 原图与缩略图先在同目录原子隔离，再事务删除索引；数据库失败时恢复文件名。
- 仍处于活动上传的截图禁止删除，只有索引和文件均验证不存在后才返回成功。
- 删除后同步清理预览缓存、时间线分页状态和今日统计。

## 2026-07-29 - 个人服务器 SFTP 手动上传第一期

- 设计、架构、API、威胁模型和项目文案统一为“选择并确认后上传到个人 SFTP 文件夹”，客户端不感知 Hermes 或 LLM。
- SQLite 移除未启用的 AI 任务表，新增单一远程配置、上传批次和逐项状态；启动时将中断任务标记失败且不自动续传。
- 私钥通过系统原生文件选择器选择并验证为受限普通文件，口令只保存到系统钥匙串。
- SSH 连接固定 SHA-256 主机指纹，支持主动读取指纹、保存配置和远端目录写权限测试。
- SFTP 上传使用确定性日期路径、随机 `.part` 文件、写入确认、显式关闭、长度核对和原子改名；同名不同长度时拒绝覆盖。
- 时间线支持逐张勾选、全选当前已加载项、数量与大小确认、批量上传和最近状态展示。
- 活动上传阻止本地删除；上传结束后允许删除并保留不含图片内容的历史。
- 前端完整检查、Rust workspace 编译与测试通过；真实 SFTP 端到端仍需隔离测试服务器验收。

## 2026-07-29 - SFTP 后台批次与非阻塞时间线

- 上传命令改为创建批次后立即返回，实际 SFTP 工作由 Rust 异步任务继续执行。
- SQLite 增加单活动批次约束、实时逐项统计和批次状态查询。
- 时间线每 800ms 刷新活动批次，上传期间仍可浏览、刷新、勾选图片或切换页面。
- 重新进入时间线时会恢复活动批次跟踪；应用重启仍将中断批次标记失败而不自动重放网络请求。
